name: OI,DC Test

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

permissions:
  id-token: write
  contents: read

env:
   AWS_REGION: ap-south-1       
#    CLUSTER: my-proj
#    NS: default

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::929677646442:role/role-ai-interview-platform 
          role-session-name: gh-oidc-test
          aws-region: ${{ env.AWS_REGION }}

    #   - name: Who am I
    #     run: aws sts get-caller-identity

    #   - name: Setup kubeconfig
    #     run: |
    #       mkdir -p ~/.kube
    #       aws eks update-kubeconfig --name "$CLUSTER" --region "$AWS_REGION"

    #   - name: What am I allowed to do
    #     run: |
    #       kubectl auth whoami                    # k8s ku enna identity-a theriyum
    #       kubectl auth can-i --list -n "$NS"

    #   - name: Allowed - should pass
    #     run: kubectl get pods -n "$NS"